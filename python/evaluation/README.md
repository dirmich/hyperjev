# Evaluation

`src/hyperjev/evaluation.py` provides hash-safe run summaries for latency,
schema validity, teacher agreement, and task-specific typed metrics. The
remaining evaluation work is data-dependent: human golden-set completion,
temporal holdout, OOD slices, and full teacher comparison reports.
